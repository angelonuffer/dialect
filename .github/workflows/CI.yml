name: CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        path: current
    - uses: actions/checkout@v4
      with:
        repository: angelonuffer/0
        ref: eb8805bb7a24318e8945581775290bba7616ce9e
        path: 0
    - uses: actions/setup-node@v4
      with:
        node-version: '22'
    - run: node 0/código/0_node.js current/tests/0 | node