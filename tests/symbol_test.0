dialect = ../code/0

test_symbol_success = dialect.analyze({
  input: "hello"
  grammar: {
    type: "symbol"
    text: "hello"
  }
})

test_symbol_failure = dialect.analyze({
  input: "world"
  grammar: {
    type: "symbol"
    text: "hello"
  }
})

test_generate_symbol = dialect.generate({
  value: "test"
  grammar: {
    type: "symbol"
    text: "test"
  }
})

{
  test_symbol_success: test_symbol_success
  test_symbol_failure: test_symbol_failure
  test_generate_symbol: test_generate_symbol
}
